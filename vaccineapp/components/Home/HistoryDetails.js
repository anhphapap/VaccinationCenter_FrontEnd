import { Text, View } from "react-native";
import React from "react";
import InjectionForm from "./InjectionForm";

const HistoryDetails = ({ route }) => {
  return <InjectionForm route={route} />;
};

export default HistoryDetails;
